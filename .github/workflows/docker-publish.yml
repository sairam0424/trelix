name: Docker Publish

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Version tag to publish (e.g. 2.9.0) — for backfilling a release whose Docker publish failed without re-tagging"
        required: true

jobs:
  publish:
    name: Build and push (extras=${{ matrix.extras }})
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        include:
          # Slim: API-embedder-only (OpenAI/Voyage/Cohere/Azure). Small image,
          # the default recommendation for anyone not running local/offline embeddings.
          - extras: serve
            tag-suffix: ""
          # Bundles sentence-transformers/torch for the local/offline embedder
          # and cross-encoder reranker — multi-GB, opt-in via the -local tag.
          - extras: "serve,local"
            tag-suffix: "-local"

    steps:
      - uses: actions/checkout@v4

      - name: Extract version
        id: version
        run: |
          if [ -n "${{ github.event.inputs.version }}" ]; then
            echo "version=${{ github.event.inputs.version }}" >> "$GITHUB_OUTPUT"
          else
            echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
          fi

      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          build-args: EXTRAS=${{ matrix.extras }}
          push: true
          # Only move :latest on a real tag push — a manual workflow_dispatch
          # backfill (e.g. re-running Docker publish for a release whose
          # build failed, without cutting a new tag) must never repoint
          # :latest at an older version than what's already published.
          tags: |
            ghcr.io/sairam0424/trelix:${{ steps.version.outputs.version }}${{ matrix.tag-suffix }}
            ${{ github.event_name == 'push' && format('ghcr.io/sairam0424/trelix:latest{0}', matrix.tag-suffix) || '' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
